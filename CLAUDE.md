# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# Проект «Pentatonic Trainer» — брифинг v1

## Текущий фокус

**Приоритет:** весь план развития (пункты 1-6) и все улучшения звука закрыты. Последними сделаны ритм-секция (бас + бочка/снейр под жанр), 3 новые прогрессии, 2 новых ритма, яркость ап-штрихов и BPM-зависимый строб. По ходу пойман и исправлен собственный регрессионный баг — highshelf на ап-штрихах, добавленный для яркости, стоял перед общим перегруз-бусом дисторшна и давал физз; яркость оставлена только для acoustic (у дисторшна голоса делят один shared-бус, точечно посветлить нельзя). Всё проверено программно (jsdom, свыше 100 assert'ов за сессию), НЕ на глаз. Задеплоено: `main@9fd853a`. **Живая приёмка ушами/глазами ещё не проведена — см. «Что осталось».**

**Что осталось — план развития закрыт, остались только приёмки и мелкие решения:**

1. **Дисторшн на слух** — Денис сообщил «стал какой-то некрасивый» до фикса физза; после фикса (`9fd853a`) ещё не переслушивал. Первое, что проверить в начале следующей сессии.
2. **Ритм-секция на слух** — бас+барабаны по жанрам, особенно реггидж (one-drop) и фанк (синкопа): ок ли грув и баланс громкости с гитарой.
3. **Строб/яркость ап-штрихов на слух** — заметна ли разница, не в перебор ли.
4. **ALL-режим с ладом на телефоне** — визуальная приёмка плотности, тянется с самой первой сессии (2026-07-14), jsdom логику подтверждает, вид — нет.
5. **Решить по пересекающимся прогрессиям** — «Соул-баллада i–III–VII–iv» и «Дорийский вамп (Oye Como Va)» по аккордам пересекаются с уже существующими записями (`i–III–iv–VII` в фанке, `i7–IV7`). Отличаются подачей/жанром/подсказкой, но не проверено с Денисом — оставить, отличить явно или убрать дубли.

**Если план развития продолжится дальше (новых пунктов пока нет)** — обсуждать новое направление с нуля под главную цель (импровизация/джем), без перегруза экрана. Заведомо НЕ добавлять: библиотеки табов/ликов, тюнер, запись себя, справочник всех ладов, теорию-простыни (см. «Чего НЕ добавлять» выше).

---

## Кто я и как мы работаем

Меня зовут Денис. Я в России, маркетолог, не разработчик. Опыта с Linux/SSH/терминалом нет.

**Правила — соблюдать строго:**
- Команды давать по одной. Денис выполняет → присылает результат → следующая
- НЕ читать файлы целиком — использовать `grep -n` для поиска нужных строк
- Перед правкой смотреть файл узким диапазоном (`view_range`), не целиком
- Не делать лишних view сразу после str_replace
- Если планируется больше 3 правок — сначала показать план, Денис подтверждает
- Краткие резюме после правок (2-3 строки), без теории если не спрашивают
- НЕ присылать файл сразу после правки — описать что сделано, спросить «заливать?»
- Музыкальные данные (боксы, аккорды, лады) — проверять программно (node/headless), а не на глаз: у гитары легко ошибиться на полутон, и это слышно

---

## Что это за приложение

**Цель — импровизация и джем на гитаре.** Всё подчинено ей: тренажёр помогает подтягивать теорию и практику именно в импровизации, а не быть справочником. Каждая новая фича проходит тест «помогает ли это прямо во время джема». Ключевая петля обучения: **слышу аккорд → вижу его тоны на грифе → целюсь в них**. Уникальная фишка — подсветка тонов текущего аккорда на грифе во время джем-лупа (учит играть «сквозь аккорды», а не просто по гамме).

**Чего НЕ добавлять (чтобы не перегрузить):** библиотеки табов/ликов, тюнер, запись себя, все семь ладов справочником без применения, теорийные тексты-простыни. Экран и так плотный.

---

## Структура

**Один самодостаточный файл** — `index.html` (~2280 строк): HTML + CSS + весь JS инлайн, без сборки, без зависимостей, без внешних библиотек. PWA-мета и inline-манифест в `<head>`.

```
index.html
├── <head>          PWA-мета, inline MANIFEST (строка 23), inline CSS
├── <body>          две вкладки: 🎸 Пентатоника | 🎵 Аккорды
└── <script>        весь JS (одна область видимости, всё глобальное)
    ├── CONSTANTS        строка 488  — CHROMATIC, OPEN_MIDI, боксы
    ├── SCALES           строка 535  — 7 ладов (мажор/минор + характерная нота)
    ├── AUDIO            строка ~600 — makeVoice, дисторшн-бас, Karplus-Strong
    ├── STATE            строка 844  — LS_KEY, saveState/loadState
    ├── RENDER           строка ~890 — гриф, селектор ладов, инфо
    ├── RHYTHMS          строка 1233 — 40 паттернов по 5 жанрам
    ├── PROGRESSION PLAYER строка ~1170 — джем-луп, метроном, подсветка тонов
    ├── PROGRESSIONS     строка 1467 — 26 минорных + 20 мажорных
    └── CHORD EXPLORER   строка 1795 — CHORD_TYPES (16), VOICINGS (120), генератор
```

---

## Ключевые структуры данных

| Структура | Строка | Содержание |
|-----------|--------|-----------|
| `CHROMATIC` | 488 | 12 нот хроматики от C |
| `OPEN_MIDI` | 489 | MIDI открытых струн `[40,45,50,55,59,64]` (0=lowE..5=highE) |
| `AM_BOXES` / `AMAJ_BOXES` | 501/516 | 5 боксов пентатоники (минор/мажор), ключ midiIdx → `[лад1, лад2]` |
| `SCALES` | 535 | 7 ладов: `base` (мин/маж пентатоника внутри), `char` (интервал характерной ноты), `charLabel`, `tip`, `genre` |
| `RHYTHMS` | 1233 | 40 боёв (blues/rock/pop/funk/reggae), `steps`+`grid`+`swing` |
| `PROGRESSIONS_MINOR` / `_MAJOR` | 1467/1501 | 26+20 прогрессий, `degrees:[{d,q}]` (d=сдвиг, q=качество) |
| `CHORD_TYPES` | 1795 | 16 типов аккордов (maj..9) с интервалами |
| `VOICINGS` | 1819 | 120 аппликатур «Нота-тип» → `[{frets:[6], desc}]` (−1=мут, 0=открытая) |

**Проверенная корректность (ревизия):** боксы пентатоники (все 12 транспозиций, полнота, стыковка), все 120 VOICINGS (правильные тоны, бас=тоника), генератор аппликатур (add9/madd9/9/dim/aug/dim7), 7 ладов (ротации мажорной гаммы), 40 ритмов (steps==grid). Единственный «флаг» — открытый C7 (x32310) без квинты, но это стандартная гитарная аппликатура.

---

## Аудио-движок

- **`getCtx()`** — ленивый AudioContext, resume при suspended.
- **Master-цепь** — мягкий компрессор-лимитер (ловит пики полифонии) → `masterOutNode` (громкость, слайдер 0–1.5). ⚠️ баг: громкость применяется ПОСЛЕ лимитера (см. баги выше).
- **`makeVoice(midi, sustain, opts)`** (строка 712) — один голос. Два тембра:
  - **acoustic** — Karplus-Strong (`ksBuffer`, строка 691): физически честный щипок, высокие затухают быстрее. ⚠️ генерируется синхронно (см. баги).
  - **distortion** — пила + суб-октава через общий перегруз-бус (`getDistBus`, строка 662): настоящая интермодуляция струн, «жужжащий» сустейн + щелчок медиатора.
- **`chick()`** — глушёный «чик» для фанка/регги. **`metroClick()`** — клик метронома.
- **Штрих боя** (`playStrumStroke`) — down = все ноты, up = верхние снизу-вверх (тише), X = чик. ⚠️ down/up одинаковы по тембру, строб фиксирован 12 мс (см. улучшения звука).

---

## Джем-луп (Progression Player)

- **`startPlayer(prog, ri, cardEl, rhythmIdx)`** (1430) → `scheduleBar` (1341) планирует такт по аудио-часам (sample-accurate lookahead, без дрожания setTimeout).
- **Подсветка тонов аккорда** (`highlightChordTones`) — при смене такта тоны текущего аккорда загораются среди нот пентатоники/лада на грифе. **Это главная обучающая фича.**
- **Режим «только ритм»** (`rhythmOnlyOn`) — луп на одной тонике, отработка боя. ⚠️ не сохраняется в LS.
- **Метроном** (`metroOn`), **тап-темпо**, **BPM-слайдер** — в раскрывающейся панели ритма у каждой прогрессии. Настройки джема по прогрессии в `jamPrefs` (LS).
- **Мёртвый код:** отсчёт «1-2-3-4» (countEvents) — вся логика есть, но не заполняется (см. баги).

---

## Состояние и хранение

- **`localStorage['penta-trainer-state']`** (`LS_KEY`, строка 844) — `saveState`/`loadState`.
- Хранится: `selKey, selBox, selMode, selScale, selGenre, timbre, noteLabels, jamPrefs, activeTab, metroOn, masterVol`.
- ⚠️ `loadState` проверяет совместимость лада с режимом (`SCALES[x].base === selMode`), несовместимый отбрасывает. `setMode` сбрасывает лад при смене минор/мажор.
- ⚠️ **В артефактах Claude.ai localStorage НЕ работает** — но это живой сайт на Netlify, там работает нормально.

---

## Деплой

**Netlify ↔ GitHub, авто-деплой** (настроено 2026-07-14):

1. Правим `index.html` локально
2. Коммитим и пушим в `main` на `github.com/enterteinment42/penta-trainer`
3. Netlify ловит push → пересобирает (билда нет, чистый HTML) → обновляет `penta-trainer.netlify.app` (1-2 мин)

- **Репозиторий:** `github.com/enterteinment42/penta-trainer` (Public), ветка `main`, один файл `index.html` в корне.
- **Netlify:** проект `penta-trainer`, команда Poigraem, Build command пусто, Publish directory пусто (корень). Раньше деплоился вручную через Netlify Drop — теперь связан с Git.
- **Без токенов через браузер:** если нет доступа к git-пушу — GitHub принимает файл drag-and-drop (Add file → Upload files → Commit), Netlify подхватит.

⚠️ **Отличие от магазина Poigraem:** здесь НЕТ backend, VPS, Supabase, БД, секретов, `.env`, параллельных сессий на VPS. Это статический фронт целиком. Ничего из инфраструктуры Poigraem сюда не переносить.

---

## Команда: /session-end

Когда Денис пишет `/session-end`:
1. Обнови раздел `## Текущий фокус` вверху файла актуальным состоянием
2. Добавь НОВУЮ запись В НАЧАЛО раздела `## Лог сессий` (не редактируй существующие!):
   - **Дата** (текущая)
   - **Что сделано** — конкретные изменения в коде/фичах
   - **Что решили** — архитектурные/продуктовые решения этой сессии
   - **Новые баги** (если нашли) — с указанием строки и описанием
   - **Открытые вопросы** — что обсуждали но не решили
   - **Что осталось** — задачи на следующую сессию
3. Если в логе стало больше 5 записей — самые старые (сверх 5) перенеси в `SESSIONS-ARCHIVE.md` (в начало файла, создай его если нет). В CLAUDE.md всегда держим 5 последних сессий.
4. **Проверка незакоммиченных/незапушенных правок:** после всех сохранений выполни `git status --short` и `git log --branches --not --remotes --oneline` (или эквивалент), покажи Денису список изменённых, но не закоммиченных файлов и коммитов, не отправленных на origin, и напомни закоммитить/запушить. Не коммить сам без явной просьбы — только напоминание со списком.
5. Спроси: «Что ещё зафиксировать?»

---

## Команда: /session-start

Когда Денис пишет `/session-start` или начинает новый чат с упоминанием проекта:
1. Прочитай раздел `## Текущий фокус`
2. Прочитай 2-3 последние записи в `## Лог сессий`
3. Резюмируй: «В прошлый раз остановились на [...]. Открыто [...]. Следующий шаг был [...].»
4. Спроси: «Продолжаем с этого или новое направление?»

---

## Команда: /session-archive

Когда Денис пишет `/session-archive`:
1. Прочитай файл `SESSIONS-ARCHIVE.md`
2. Резюмируй ключевые моменты — решения, баги, паттерны
3. Спроси: «Что именно ищем?»

---

## Команда: /audit

Когда Денис пишет `/audit` — техническая ревизия приложения:
1. Прочитай `index.html` секциями (структуры данных → аудио → рендер → плеер)
2. **Музыкальные данные проверяй программно** (node): боксы пентатоники (ноты в ладу, полнота, стыковка), VOICINGS (тоны аккорда, бас), лады (ротации мажорной гаммы), ритмы (steps==grid). Не на глаз.
3. **UI/поведение проверяй в jsdom** headless (клики по кнопкам, смена тональностей, restore состояния из localStorage) с конкретными assert'ами.
4. Формат находок: пронумерованные (🔴 критично / 🟡 важно / 🟢 отложить), с указанием строки, что не так, как чинить.
5. Всегда держи в голове главную цель — импровизация и джем. Оценивай не только баги, но и «служит ли это цели, не перегружает ли».

---

## Лог сессий

### 2026-07-17 (сессия 3 — ритм-секция, точечные прогрессии/ритмы, звук, фикс регрессии дисторшна)

**Контекст:** Денис попросил доделать оставшиеся правки из плана развития. На развилке «за какую задачу берёмся» выбрал ритм-секцию (пункт 2 плана) — предложена как рекомендация с обоснованием (самый большой качественный скачок для джема), выбрана явно. На двух уточняющих вопросах по реализации выбрал «барабаны под жанр» (не один универсальный бэкбит) и «тумблер выключен по умолчанию» (оба — рекомендованные варианты). После ритм-секции получил разрешение продолжать автономно («после этого самостоятельно переходи к следующим правкам») и добил оставшиеся пункты плана без дальнейших вопросов. В конце Денис сообщил, что дисторшн «стал какой-то некрасивый» — заметил регрессию от правки этой же сессии, зафиксирован и исправлен инцидент.

**Что сделано:**
- **Ритм-секция** (`09aef11`): тумблер «🥁 Ритм-секция» в панели ритма (по умолчанию ВЫКЛ, `bandOn` в localStorage). Три новых генератора Web Audio без семплов: `kick` (синус 120→45Гц питч-спад), `snare` (полосовой шум + тоновое тело), `bassNote` (пила+суб через лоупас). Таблица `BAND` — раскладка по долям такта под жанр: блюз/рок/поп — бэкбит (бочка 1/3, снейр 2/4), фанк — синкопа, реггидж — one-drop (бочка+снейр только на 3-й доле, бас на офф-битах). Бас берёт тонику/квинту текущего аккорда на октаву ниже.
- **Прогрессии** (`6d40ae1`): 16-Bar Blues (минор, блюз), «Соул-баллада i–III–VII–iv» (минор, поп), «Дорийский вамп (Oye Como Va)» (минор, фанк, i7–IV7 — мажорный IV подсвечивает натуральную 6-ю ступень).
- **Ритмы** (`6d40ae1`): «6/8 Баллада» (grid 12) и «Простые четверти» (grid 4) в жанр pop, для новичкового темпа.
- **Звук** (`6d40ae1`, частично отменено в `9fd853a`): строб между струнами теперь зависит от BPM и жанра (шире на медленном темпе, вдвое уже на фанке/реггидже) вместо фиксированных 12мс. Яркость ап-штрихов (highshelf-биквад) добавлена сначала в обе ветки тембра (acoustic + distortion).
- **Фикс регрессии дисторшна** (`9fd853a`): Денис сообщил, что дисторшн стал «некрасивый» — highshelf на ап-штрихах в distortion-ветке стоял перед общим перегруз-бусом (`getDistBus`), бустил ровно те верха, которые `preLp` (2800Гц) специально срезает перед сатурацией, получался физз. Убран из distortion-ветки полностью (там все голоса делят один shared-бус — точечно посветлить один голос архитектурно нельзя); в acoustic-ветке оставлен (там у каждого голоса своя цепь фильтров, работает чисто). В дисторшне ап/даун снова различаются только громкостью, как до регрессии.
- Каждая правка проверена программно (jsdom с моком AudioContext, экспорт внутренних функций через `globalThis.__T` перед незатронутым кодом) — 73 assert'а + smoke-тест полной загрузки страницы за сессию, ни одной проверки «на глаз». jsdom установлен в scratchpad (не в проект), тестовые файлы туда же — в `index.html` тестовый код не просачивался (проверено grep'ом перед каждым коммитом).

**Что решили:**
- Порядок задач плана переподтверждён явно через AskUserQuestion на каждой развилке (какую задачу брать, барабаны под жанр vs один паттерн, тумблер вкл/выкл по умолчанию) — не выбирал молча.
- После первой развилки Денис явно разрешил дальше работать автономно до конца сессии — так и было сделано, остальные пункты плана добиты без дополнительных вопросов.
- Яркость ап-штрихов — фича, специфичная для акустического тембра; для дисторшна корректной реализации через общий перегруз-бус нет, поэтому не пытались обойти архитектуру, а просто отключили там, где физически ломает звук.
- Новые прогрессии — не проверяли явно с Денисом пересечение по аккордам с существующими (см. открытые вопросы).

**Новые баги:** регрессия дисторшна из этой же сессии (см. выше) — пойман Денисом на слух, не тестами (jsdom проверяет граф аудио-узлов, не субъективное качество тембра), исправлен в течение той же сессии.

**Открытые вопросы:**
- Дисторшн после фикса ещё не переслушан Денисом («проверим позднее»).
- Ритм-секция, строб/яркость — не проверены на слух.
- «Соул-баллада» и «Дорийский вамп» пересекаются по набору аккордов с существующими прогрессиями (`i–III–iv–VII` фанк, `i7–IV7` фанк) — оставить, дифференцировать явно или убрать дубли не решено.
- Визуальная приёмка ALL+лад на телефоне — тянется с 2026-07-14, всё ещё не проведена.

**Что осталось:**
1. Живая приёмка ушами/глазами: дисторшн (первое), ритм-секция, строб/яркость, ALL-режим на телефоне
2. Решить судьбу пересекающихся прогрессий (5 и 6 из плана — теперь пересмотреть, а не добавлять новые)
3. Новых пунктов плана развития пока нет — при следующем большом шаге обсуждать направление с нуля, держа в голове «не перегружать экран»

### 2026-07-14 (сессия 2 — все 6 багов ревизии + фичи «Лики/ходы» и «Блюзовая b5»)

**Контекст:** Денис попросил чинить баги ревизии по два за раз («делай 1 и 3», затем «делай 2 и 4»), потом добавить оставшиеся мелкие (5 и 6, с уточнением — отсчёт сделать опциональным), затем перешёл к фичам плана развития: сначала спросил, что осталось (напомнил про пассажи/лики), выбрал пункт 4 (лики/ходы), затем пункт 1 (блюзовая b5).

**Что сделано:**
- **Баги 1+3** (`d2e46a2`): `degreeRootMidi()` — октава ступени фиксирована ±полоктавы от тоники вместо цепочки от предыдущего аккорда (выбрано явно из двух вариантов через AskUserQuestion); громкость перенесена до компрессора.
- **Баги 2+4** (`270dec2`): резайз/поворот перерисовывает SVG-рамки боксов (дебаунс 120мс); `ksBufferCached` — LRU-кэш Karplus-Strong буферов (64 записи, квантование длительности до 0.1с).
- **Баги 5+6** (`d96c2db`): опциональный тумблер отсчёта «⏱ Отсчёт» (по умолчанию выключен — явная просьба Дениса, не всегда включённый count-in); `rhythmOnlyOn`/`countInOn` в localStorage; создан реальный `sw.js` (network-first, выбран явно вместо удаления регистрации).
- **Фичи «Лики/ходы» + «Блюзовая b5»** (`06c6c9a`, залиты одним коммитом): блок «4. Лики / ходы» (прогрессии → «5.»), 6 паттернов с подсветкой+звуком на грифе, автопереключением бокса и синхронизацией старта со следующим тактом джем-лупа; `makeVoice` получил `opts.bend` (плавный подъём высоты через `detune`) для бендов/слайдов; кнопка «♭5 BLUES» добавляет тритон своим цветом, только в MINOR, аккуратно не дублирует локрийскую характерную ноту (тот же pitch-class).
- Каждая правка проверена программно (node для музыкальных данных: все прогрессии × 12 тональностей; jsdom для поведения UI) — свыше 100 assert'ов за сессию, ни одной проверки «на глаз».

**Что решили:**
- Октавы корней — «фиксировано от тоники», а не «плавно от предыдущего аккорда» (Денис выбрал явно; альтернатива красивее на нисходящих ходах, но даёт скачки 8-11 полутонов раз в круг на некоторых прогрессиях).
- Отсчёт «1-2-3-4» — только по явному включению, не всегда (иначе мешал бы тем, кто уже приготовился).
- `sw.js` — создать реальный файл, а не убирать регистрацию (офлайн — часть обещания PWA-баннера).
- Лики — подсветка **и звук** вместе (не только визуал), блок между грифом и прогрессиями, скрыт в MAJOR (лики построены на минорной пентатонике).

**Новые баги:** в ходе разработки отсчёта пойман собственный баг новой фичи (не из старой ревизии) — бейдж «4» не гас после отсчёта, потому что код сверял с `player.nextBarTime` (который после отсчёта уже равен старту первого такта). Исправлено добавлением отдельного `player.countEndTime` до деплоя — поймано тестом, не Денисом.

**Открытые вопросы:**
- Визуальная приёмка ALL-режима с ладом на телефоне так и не проведена (тянется со 2026-07-14 сессии 1).
- Порядок оставшихся фич не переподтверждён явно — ритм-секция предложена как следующий приоритет, Денис не возражал, но и не выбирал её напрямую среди альтернатив.

**Что осталось:**
1. Ритм-секция: бас на тонике-квинте + бочка/снейр — следующий приоритет по плану
2. Точечное добавление прогрессий (дорийский фанк, 16-тактовый блюз, соул-баллада), 5-7 штук
3. Точечное добавление ритмов (6/8 баллада, простые четверти)
4. Улучшения звука по мелочи: тембр ап-штрихов, строб от BPM/жанра
5. Визуальная приёмка ALL+лад на реальном телефоне (хвост)

### 2026-07-14 (сессия 1 — ревизия приложения + фича «Гаммы / лады» + настройка деплоя Netlify↔GitHub)

**Контекст:** Денис попросил прогнать приложение через полную техническую ревизию (техдолг, нереализованный потенциал, ошибки, звук, база аккордов), обсудить развитие под главную цель — импровизацию и джем, без перегруза. Затем поэтапно дополнял план развития (пассажи/вертушки/«фишки»; обучение ладам). Выбрал к реализации пункт 3 (гаммы/лады). После — настроили деплой: репозиторий на GitHub + связка с существующим Netlify-проектом для авто-деплоя. В конце — создание этого CLAUDE.md по аналогии с магазином Poigraem.

**Что сделано:**
- **Полная ревизия `index.html`** — программная проверка всех музыкальных данных (боксы, 120 VOICINGS, генератор, ритмы). База в отличном состоянии, ошибок нот нет. Найдены 6 багов (регистровые скачки прогрессий, ресайз рамок, громкость после лимитера, синхронный KS, мёртвый count-in, мелочи) + улучшения звука. Всё зафиксировано в «Найденных багах» выше.
- **Фича «Гаммы / лады»** (пункт 3) — новый блок «3. Расширь до гаммы / лада»: `SCALES` (7 ладов, строка 535), селектор `renderScaleSel`, добавленные ноты гаммы (фиолетовые `.nb.ext`) и характерная нота лада (оранжевая `.nb.char`) поверх пентатоники, карточка `renderScaleInfo` (ноты + жанр + подсказка «как обыгрывать»), легенда, интеграция с подсветкой тонов аккорда. Прогрессии перенумерованы (3→4). Локрийский честно убирает квинту с грифа. selScale в localStorage со сбросом при смене минор/мажор.
- **Проверка ~30 assert'ов в jsdom** (клики по UI, смена тональностей, restore, битое состояние) + программная проверка, что все 7 ладов — корректные ротации мажорной гаммы. Пойман и исправлен баг ALL-режима в процессе.
- **Деплой Netlify↔GitHub** — создан репозиторий `github.com/enterteinment42/penta-trainer`, файл залит через браузер (drag-and-drop, без токена), существующий Netlify-проект `penta-trainer` (раньше Netlify Drop) связан с Git через Continuous deployment → Link repository. Проверено: файл в репо идентичен оригиналу (`git clone` + `diff`), деплой прошёл (`main@d66b35c`).
- **Создан этот `CLAUDE.md`** — адаптация брифинга Poigraem под одно-файловый проект без backend: убраны разделы VPS/Supabase/безопасности/параллельных сессий, сохранены команды сессий (`/session-start`, `/session-end`, `/session-archive`), добавлена команда `/audit`.

**Что решили:**
- Главная цель приложения — импровизация и джем, всё ей подчинено. Каждая фича проходит тест «помогает ли во время джема».
- Гаммы/лады подаются не как справочник, а через настроение + жанр + характерную ноту, с несколькими уровнями глубины (минор+2 ноты → полный лад). Объединили бывшие пункты 3 и «обучение ладам» в один.
- Музыкальные данные проверяются программно, не на глаз (легко ошибиться на полутон).
- Деплой Public-репозиторием (нет ничего чувствительного, проще для Netlify).

**Новые баги:** баг ALL-режима с ладом (окно ext-нот) — найден и исправлен в этой же сессии до деплоя.

**Открытые вопросы:**
- Визуальная приёмка плотности ALL-режима с ладом на реальном телефоне не проводилась (jsdom подтверждает логику, но не вид).
- 6 найденных багов ревизии ещё не исправлены — порядок предложен (первым регистровые скачки прогрессий как самое слышимое).

**Что осталось:**
1. Визуальная приёмка ALL-режима с ладом на телефоне
2. Баги ревизии по порядку: (1) октавы корней прогрессий → (3) громкость/клиппинг → (2) ресайз рамок → (4) кэш KS
3. Следующие фичи развития: блюзовая нота (b5) → ритм-секция (бас+барабаны) → лики/вертушки → точечные прогрессии/ритмы
